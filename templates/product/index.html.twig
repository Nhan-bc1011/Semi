{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-sm-4">
        <div class="bs-example" data-example-id="striped-table">
        <table class="table table-striped">
            <thead>
            <tr>
{#                <th>ID</th>#}
                <th>Category</th>

            </tr>
            </thead>
            <tbody>
            {% for category in categories %}
                <tr>
{#                    <th scope="row">{{ category.id }}</th>#}
                    <td><a href="{{path('productByCat',{'id':category.id})}} ">{{ category.catname }}</a></td>

                    <td>
                        {#                        <a href="{{ path('todo_details', {'id': product.id}) }} " class='btn btn-success'>View</a>#}
                        {#                        <a href="{{ path('todo_edit', {'id': product.id}) }}" class='btn btn-default'>Edit</a>#}
                        {#                        <a href="{{ path('todo_delete', {'id': product.id}) }}" class='btn btn-danger'>Delete</a>#}
                        <a href="{{path('productByCat',{'id':category.id})}} " class='btn btn-success'>View</a>
                        <a href="{{ path('product_editcat', {'id': category.id}) }}" class='btn btn-default'>EditCat</a>
                        <a href="{{ path('product_deletecat', {'id': category.id}) }} "class='btn btn-danger'>Delete</a>
                    </td>
                </tr>
            {% endfor %}

            </tbody>
        </table>
        </div>
        </div>
        <div class="col-sm-8">
        <div class="bs-example" data-example-id="striped-table">
        <table class="table table-striped">
            <thead>
            <tr>
{#                <th>ID</th>#}
                <th>Product Name</th>
                <th>Price</th>
                <th>Quantity</th>
                <th>Description</th>
                <th>Image</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% for product in products %}
                <tr>
{#                    <th scope="row">{{ product.id }}</th>#}
                    <td>{{ product.proname }}</td>
                    <td>{{ product.price }}</td>
                    <td>{{ product.quantity }}</td>
                    <td>{{ product.description }}</td>
                    <td><img src="{{ asset('upload/productImages/' ~ product.image) }}"width="180px" height="180px" ><td>
                    <td>
                        {#                        <a href="{{ path('todo_details', {'id': product.id}) }} " class='btn btn-success'>View</a>#}
                        {#                        <a href="{{ path('todo_edit', {'id': product.id}) }}" class='btn btn-default'>Edit</a>#}
                        {#                        <a href="{{ path('todo_delete', {'id': product.id}) }}" class='btn btn-danger'>Delete</a>#}
                        <a href="{{path('product_details',{'id':product.id})}} " class='btn btn-success'>View</a>
                        <a href="{{ path('todo_edit', {'id': product.id}) }}" class='btn btn-default'>Edit</a>
                        <a href="{{ path('product_delete', {'id': product.id}) }} "class='btn btn-danger'>Delete</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
        </div>
    </div>
{% endblock %}